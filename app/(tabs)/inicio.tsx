import { router } from "expo-router";
import { Button, Text, View } from "react-native";

const inicio = () => {
    return (
        <View>
            <Text>inicio</Text>
        </View>
    );
};

export default inicio;
